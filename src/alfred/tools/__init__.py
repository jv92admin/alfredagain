"""
Alfred V2 - Tool Definitions.

All tools follow these rules:
1. Return EntityRefs (or lists of them)
2. Accept IDs for operations, not raw strings
3. Never let LLMs fabricate IDs
4. Never let LLMs construct SQL
"""

